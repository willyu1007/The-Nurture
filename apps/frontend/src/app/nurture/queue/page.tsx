import { Scene } from "@willyu1007/web-workbench";

// Placeholder so the hub's attention rows have a destination until P5 builds
// the real queue.
export default function QueuePage() {
  return (
    <Scene>
      <p className="mt-body">按阻塞归属分组的流程队列将在此处呈现。</p>
    </Scene>
  );
}

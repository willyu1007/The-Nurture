{
  "name": "@the-nurture/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The Nurture dev host harness (Fastify). Wires the My-Chat workflow-runtime worker/services + Postgres ports + the nurture scenario module to run family_rule_trial end-to-end. Replaced by the real My-Chat host post-MVP.",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@my-chat/workflow-contracts": "file:../../../My-Chat/packages/workflow-contracts",
    "@my-chat/workflow-runtime": "file:../../../My-Chat/packages/workflow-runtime",
    "@the-nurture/db": "workspace:*",
    "@the-nurture/scenario": "workspace:*",
    "dotenv": "^16.4.0",
    "fastify": "^5.2.0"
  }
}

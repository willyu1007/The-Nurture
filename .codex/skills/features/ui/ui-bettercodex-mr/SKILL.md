---
name: ui-bettercodex-mr
description: Prevents generic AI/Codex UI patterns when generating frontend code using Morethan-aligned color rules and calmer, human-designed UI decisions.
category: features/ui
ssot_path: .ai/skills/features/ui/ui-bettercodex-mr
---

# ui-bettercodex-mr (entry)

Canonical source: `.ai/skills/features/ui/ui-bettercodex-mr/`

Open `.ai/skills/features/ui/ui-bettercodex-mr/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.

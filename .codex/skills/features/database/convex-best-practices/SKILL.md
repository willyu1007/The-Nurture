---
name: convex-best-practices
description: Apply Convex-specific coding and review rules when building or reviewing Convex-backed features. Treat convex/schema.ts as DB SSOT, keep public functions validated and authenticated, prefer indexes over query filters, commit generated code, and keep docs/context contracts synchronized.
category: features/database
ssot_path: .ai/skills/features/database/convex-best-practices
---

# convex-best-practices (entry)

Canonical source: `.ai/skills/features/database/convex-best-practices/`

Open `.ai/skills/features/database/convex-best-practices/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.

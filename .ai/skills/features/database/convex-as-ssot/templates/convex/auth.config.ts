import type { AuthConfig } from "convex/server";

/**
 * Optional Convex auth configuration.
 *
 * Replace the provider configuration with the auth provider used by the project.
 * If the project does not use Convex auth yet, keep this file as a placeholder.
 */
const config: AuthConfig = {
  providers: [],
};

export default config;

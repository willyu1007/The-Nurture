version: 1
secrets:
  database_url:
    backend: env
    ref: env://DATABASE_URL
    hint: Inject the production Nurture database URL through the deployment environment.
  nurture_internal_service_token:
    backend: env
    ref: env://NURTURE_INTERNAL_SERVICE_TOKEN
    hint: Reserved for a later approved non-development activation.

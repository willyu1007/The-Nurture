# C30-I3 Scenario-owner Adoption Default-off Acceptance Record

> Historical acceptance notice: the later quality review reopened this result.
> Artifact 61 records the repaired successor source, lock and current
> reacceptance; this file remains the first I3-G closure record only.

## Decision

- Date: 2026-08-06
- Task: `M-002 > F-002 > T-002 nurture-institution-mode`
- Scope source: artifact 53
- Ordered implementation records: artifacts 54 through 59
- Convergence source: `60ee4a48a83df2ab46247cc0a92d47ccecc7bf43`
- Final source candidate: `c8b9ce2fa203d7f51c856dcbf21d062bb09d8f12`
- Final lock commit: `15207ba6feaa5b5d3cb87ea868be353add02f720`
- Aggregate source hash:
  `5c08b542d47e60cbd189a3b0ea573b9cd10ec3f2ec0a0d9b28053a8363cc4ab6`
- State: `C30_I3_SCENARIO_OWNER_ADOPTION_ACCEPTED_DEFAULT_OFF /
  I4_SCOPE_REVIEW_ELIGIBLE`

C30-I3 is accepted. Ordered I3-A through I3-G establish the Nurture-owned
Scenario boundary for exact upstream contracts, detached request trust, typed
Participant and canonical pair association, action-free subject presentation,
generic direct/claimed execution and authoritative protected-content lifecycle.
The implementation remains mechanically default-off and has no production
action or protected declaration.

## Exact dependency and source convergence

- Base remains clean at `4350086993d837baa8030564f4e19593dedd96b0`,
  whose accepted contract source is `15ff031…` and aggregate is
  `d17f2358…6383`.
- My-Chat remains clean at `cd7bbc2623dff8621c2c7155b04d1bf759e8404a`,
  whose repaired runtime is `658b897…` and aggregate is `8172e370…10a5ad`.
- The Nurture lock pins seven named profiles, rejects dirty/symlinked/escaping
  inputs and passes twice consecutively at aggregate `5c08b542…c4ab6`.
- YAML is the only editable manifest source; generated parity is clean and one
  canonical default-off module is exported. Package-local Scenario tests now
  resolve the repository root explicitly, so the declared package test command
  actually executes its complete population.

## Production prefix and isolated complete graph

- Production declares exactly the trusted-invocation plus subject-presentation
  dependency-complete prefix and its two matching sources.
- Production domain-action and protected-interaction arrays are empty; the one
  presentation has `action_offer_policy=none` and no action keys.
- The isolated test-only complete graph adds the exact Base action/protected
  dependencies, both `scenario_direct_empty_v1` and
  `workflow_claimed_step_v1` neutral declarations and one protected contract.
  Base's runtime manifest validator accepts it without mutating production.
- The maintained default-off census hash is
  `448d37e15ffe356e2db6e02d780fa66e5811f977df050ff71e401054b4483c3e`.
  Enabled manifest capabilities, production action/protected rows, action
  offers, positive C30 application/internal routes and Workspace activation
  models all count zero.

## Fresh database qualification and cleanup

- Exact target: disposable `nurture-c30-i3`, PostgreSQL 16/pgvector on
  `127.0.0.1:55440`; unrelated 55439 owner was observed and never touched.
- The authorized database was dropped and recreated inside that exact container.
  All 19/19 migrations applied from empty, including the three additive I3
  migrations. Database-to-SSOT and migrations-to-SSOT diffs are empty.
- Prisma validate and generate pass. The complete DB population passes 24 files
  / 253 tests against the final committed candidate.
- Before cleanup all eight `nurture_c30_*` tables were truncated and proved at
  zero rows. The exact container was destroyed; no container publishes 55440
  and no process listens on that port. No existing database was contacted.

## Cumulative verification

| Check | Result |
| --- | --- |
| Root TypeScript check | PASS |
| Complete Scenario unit/conformance | PASS — 58 files / 635 tests |
| Scenario-service regression | PASS — 8 files / 52 tests |
| Complete PostgreSQL regression | PASS — 24 files / 253 tests |
| Scenario/DB/scenario-service/frontend builds | PASS |
| Frontend ESLint and Stylelint | PASS |
| Test routing | PASS — 104 files: 58 unit / 24 DB / 11 dev-host / 10 scenario-service / 1 X5 |
| Persistence/G2 regression/manifest generation | PASS |
| Prisma validate/generate and both schema diffs | PASS |
| Strict context and project governance lint | PASS |
| Exact upstream, source portability and repeated lock | PASS |
| Default-off static/route/schema census | PASS — all positive populations zero |
| Disposable database cleanup | PASS — C30 rows zero, container absent, 55440 free |

The root umbrella `pnpm build` was also probed. It stopped before compilation at
the intentionally historical G1 verifier, which requires Base `06303e9…` and
correctly rejects the C30 topology's `4350086…`. I3 did not rewrite that old
evidence. The C30 upstream verifier plus direct Scenario/DB/scenario-service and
frontend build gates above provide the current-topology build qualification.

During final qualification three convergence defects were repaired: the
package-local Scenario test command previously found zero tests, the routing
census still expected pre-I3 populations, and root TypeScript exposed readonly
test tuples incompatible with the mutable Base pair wire. The repaired command,
census and explicitly typed fixtures all pass in the sealed candidate.

## Effect boundary and next gate

No Base/My-Chat source, existing database, production action/protected
declaration, product route, secret/environment value, capability/Workspace
activation, deployment, I4, C31-C35, T-008, Pilot or traffic operation occurred.
I3 closure opens only a separately authorized C30-I4 scope review; it does not
authorize I4 implementation.

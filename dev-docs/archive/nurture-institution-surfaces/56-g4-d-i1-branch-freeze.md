# G4-D Enrollment Journey — I1 Branch Freeze

## Verdict

- Date: 2026-08-09
- Task: T-007
- Input: `G4_0E_EXIT_PASS` ([`55`](./55-g4-0e-exit-record.md))
- Verdict: `G4_D_I1_BRANCH_OPEN`
- First increment: 0E-1 registry/state guard/role-safe projection only

## Ordered implementation

1. one-item registry, exact stage/waiting/pending/lifecycle/milestone unions,
   invariant guard and body-free projection;
2. private workflow/inquiry/touchpoint carrier plus command/repository and
   migration;
3. waitlist/policy/offer/reservation/preparation cancellation;
4. phase migration and trial start/review/extend/end transactions;
5. formal proposal/current-owner adapter/formalization/completion;
6. I2 public contract rotation, then I3 disposable owner qualification and I4
   joint conformance under their separate gates.

Each increment must keep the canonical manifest/module free of the Workflow
until I2. A higher increment cannot replace a missing earlier owner fact with a
second field, cached projection, timer or generic state.

## Increment 1 acceptance

- exactly one registered domain definition in local source;
- ordinary actions/signals are rejected as Workflow types;
- exact unions match 0E-1 and invalid terminal/pending combinations fail
  closed;
- `capacity_waitlist` exists only in the stage union;
- projection contains fixed safe text, the exact My-Chat-owned opaque workflow
  Run `CanonicalRef`, versions and no private Nurture workflow ref,
  inquiry/contact/body or Host runtime internals;
- Admin mobile capability refs are empty;
- current manifest/module contain no `EnrollmentJourneyWorkflowV1`;
- targeted unit tests and scenario TypeScript typecheck pass;
- no Prisma/migration or external checkout change.

## Rollback

Increment 1 is one exported pure module plus tests and docs. Reverting it has no
database, manifest or external-owner effect.

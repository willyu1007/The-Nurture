# T-009 Verification

Evidence accumulates per increment (see `01-plan.md` DoD lines). Empty until
the first increment lands.

## 2026-08-13 maintenance requalification

- The repository-wide current typecheck exposed two archived T-009 fixtures
  that no longer supplied the now-required current owner-evidence expiry and
  guardian-role identity. Both fixtures were repaired without changing
  production semantics.
- `pnpm typecheck` and the complete 97-file / 1083-test unit lane passed after
  both Prisma clients were regenerated.

| Increment | Gate | Evidence | Result |
| --- | --- | --- | --- |
| I1 | `pnpm test:unit` incl. JCS vectors + schema-shaped envelope fixtures | 55 files / 603 tests green (24 new across jcs/assembler/receipt suites); digest parity test replicates the consumer verifier algorithm; `tsc --noEmit` adds zero new errors (one pre-existing live-sibling drift error, see notes) | PASS |
| I2 | migration deployed, zero drift, boundary green, `pnpm test:db` green | 17/17 migrations; `migrate diff` empty; boundary 63 tables / 93 enums; 23 files / 238 DB tests incl. `t009-family-growth-outbox` (tx atomicity both rollback directions, released-once partial unique, kind/source + digest + receipt-companion CHECKs, claim/backoff/settle, receipt replay no-op); evidence in `artifacts/db/i2/` | PASS |
| I4 | resolver unit suite all deny reasons + integration over real binding rows | 9 unit tests (every deny reason, boundary expiry, exchange-not-called-on-local-deny) + 7 integration tests (healthy resolve with owner-ref handshake, missing/revoked/quarantined/expired chains, target mismatch, exchange unavailable); `pnpm test:unit` 612 green | PASS |
| I7a (N8 conformance) | all twelve fixtures through the real provider chain + real HTTP vs contract-faithful double; zero contract violations | 10 e2e cases green (single-family loop; pending confirmation; exact replay → duplicate w/ original refs; source-key digest conflict; correction/removal/redaction ordered; lifecycle-first suppression tombstones the late release; two-family independence under the privacy gate; unauthorized-second-child fail-closed; expired+missing binding deny; 503→retry→success with frozen backoff and attempt counts); suites 621 unit / 258 DB / 66+65 scenario-service green; tsc clean | PASS |
| I3b (worker) | engine decisions + worker consequence recording + stale-claim reclaim | 6 engine unit tests (backoff curve/cap/jitter bounds, settle-only-on-valid-receipt incl. wrong-event receipt, attention threshold) + 5 worker tests (claim lease window, receipt mapping, rejected settles failed, frozen backoff on transport error, attention event) + DB stale-claim integration test; scenario-service suite 11 files / 66 tests green | PASS |
| I5 (rendition) | frozen §4/§5 behavior over real HTTP + real rows | 4 controller e2e tests (401 taxonomy, 400/404 collapse, resolve→download→revoke-mid-lease 404, storage-unbound 503) + runtime unit tests (dual-token rotation, lease roundtrip/expiry/tamper/foreign-key) + 5 DB integration tests (resolve with facts, ref parsing, never-released null, removed/redacted deny, revision-drift/lifecycle deny) | PASS |
| I3c (preparer) | prepared emission from real rows accepted end-to-end by the commit; every deny reason over real rows | `t009-family-growth-preparer` 6 integration tests (full prepare→commit→outbox loop with production-form `familyRefKey`; binding_missing passthrough; unresolved-schedule policy identity; media digest/mime/drift/empty each `media_facts_unavailable`; unsealable title; unknown revision); suites 615 unit / 252 DB green after the review-fix pass | PASS |
| I6.2 (queue vocabulary) | contract tooling + conformance + harness/db projection tests | `familyGrowthQueueState` in `publish-process-types` + optional `familyGrowth` on the queue item at `1.16.0` (no version rotation — additive, generated restored to baseline then rebuilt once); harness projection test + `publish-lane.read` receipt-join integration cases; 14 publish-family slice hashes re-frozen; suites 615 unit / 256 db / 66+64 scenario-service green | PASS |
| I7b (joint suite) | the N8 fixtures against the REAL My-Chat consumer + real database, both directions of the wire | 7 joint cases green in the x5 lane (`t009-family-growth-joint.integration.test.ts`): admit with imported media digest-verified end-to-end through the real rendition exchange; replay answers wire `duplicate` under the original receipt id and the provider store keeps exactly the applied row; guardian-confirmation rejected `contract_invalid`; source-key digest conflict; correction/removal/redaction applied in order (material `redacted`); lifecycle-first suppression tombstones the late release; membership-less family rejected `family_membership_required` while the sibling family applies; 503 downtime retried on the frozen backoff to success. Fixtures 10/11 I7a-proven (noted in header). Routing census 57/26/11/14 + x5-joint=2 | PASS |
| I8 quality pass | independent Codex (gpt-5.6-sol) + self review of the I8 range; every finding fixed with a regression test | 3 review findings + 1 self-found fixed: bounded two-query page join (released rows + per-release lifecycle groupBy — unbounded correction history can no longer bloat a page read; composite string keys eliminated), raw NUL bytes removed from source, the I8 test rebuilt to catch last-wins/per-process-keying/unmapped-removal/existence-bit defects (two targets, precedence both directions, head moves on every append), vocabulary scan normalized against snake/camel/spaced variants with structural enum whitelisting. Lanes 616/257/66+64/12 green; requal re-run at the fixed checkpoint | PASS |
| I8 (queue binding) | surface conformance over the new states + no-family-archive invariant re-asserted | `1.17.0` rotation (digest `d22851d9…`, shared-core unchanged `7bd8a82d…`); lifecycle overlay (`correction_appended`/`target_removed`/`redacted`) on the queue `familyGrowth` entry, projected from committed lifecycle outbox rows with redacted>removed>correction precedence and folded into the source head (per-kind census); harness passthrough test + DB integration test (overlay appears, head moves per append, precedence flip); NEW phase-2 scan proves the queue/release contract text carries no family-archive vocabulary (family_id/child_id/companion refs/growth_material/cultivation/guardian beyond the frozen receipt status). Lanes 616 unit / 257 db / 66+64 scenario-service / 27 dev-host / 12 x5 green; deterministic rebuild ok; self-pin re-frozen `8717270f…` | PASS |
| Post-requal quality pass | independent Codex (gpt-5.6-sol) + self review of the I6.2+I7b range; every finding fixed with a regression test | 4 confirmed findings fixed: family-growth census folded into the queue source head (heads-move-on-receipt test), latest-receipt selection made deterministic on `processedAt` (multi-receipt display test), J9 rebuilt as one-workspace split-process independence with distinct bytes and zero-side-effect assertions, JX1 tampered-rendition case proves the consumer's own digest check (`media_import_mismatch` + discard + nothing staged). Lanes: 615 unit / 256 db / 66+64 scenario-service / 12 x5 | PASS |
| I6-batch requalification | full gates on exact detached topology, empty DBs | `06-i6-batch-requalification-record.md`: three adjacent detached worktrees (Nurture `97b9afe`, My-Chat `df7a273`, Base `8a3ea90`), two disposable tmpfs databases migrated from empty (18 Nurture / 29 My-Chat migrations, zero diff), pin verifier + all assert scripts + deterministic contract rebuild + tsc + 615 unit / 256 db / 66+64 scenario-service / 27 dev-host / 11 x5-joint all green; default-off census re-proven; topology destroyed cleanly | PASS |
| I3 (non-wire) | release+outbox and lineage+outbox pair atomicity, replay identity, default-off parity | `t009-family-growth-emission` 8 integration tests (committed triple with schema-valid stored envelope binding exact row ids and verifiable digest; exact replay appends nothing; invalid emission rejects write-free incl. freeze rollback; no-emission path emits zero rows; lifecycle envelope target copied from storage; skip when release never delivered; correction-without-text fails the pair closed; redaction without body) + 3 harness tests (denied → `binding_unavailable` before commit, prepared rides into commit input, absent preparer keeps exact G3-D input shape); suites 615 unit / 246 DB green; `tsc` zero new errors | PASS |

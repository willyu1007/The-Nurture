# Staging owner-service planning record.
# No runnable Nurture deployment artifact is registered.

environment: staging
status: planned
deployModel: vm
platform: aliyun-ecs+docker-compose
debug: false
logLevel: info

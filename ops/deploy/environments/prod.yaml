# Production owner-service planning record.
# No runnable Nurture deployment artifact is registered.

environment: prod
status: planned
deployModel: vm
platform: aliyun-ecs+docker-compose
debug: false
logLevel: warn

# Development owner-service planning record.
# No runnable Nurture deployment artifact is registered.

environment: dev
status: planned
deployModel: vm
platform: aliyun-ecs+docker-compose
debug: true
logLevel: debug
